{
  "name": "figma-sync-plugin",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=iife --target=es2017 --platform=browser --minify"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
